```markdown
# Contributing

This repo is a reference scaffold. To contribute:

- Run services locally with Docker Compose:
  docker-compose up --build

- Work on server:
  cd server
  npm run dev

- Work on web:
  cd web
  npm run dev

Keep changes focused and include tests for new server endpoints. Use PRs and small reviewable commits.
```